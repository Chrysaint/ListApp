import styled from "styled-components";
import * as styles from "@/app/styles/variables";

export const NotFound = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: ${styles.FONT_SIZE.lg};
  color: ${styles.COLOR.accent};
`;
